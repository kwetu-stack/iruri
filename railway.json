{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "preDeployCommand": ["python scripts/release.py"],
    "startCommand": "python scripts/release.py && gunicorn wsgi:app"
  }
}
